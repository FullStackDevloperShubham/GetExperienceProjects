// Write a program that converts Celsius to Fahrenheit and vice versa using a variable for input.
// The program should output the results in the console.
let celsius = 30;
let fahrenheit = (celsius * 9/5) + 32;
console.log(`${celsius}°C is ${fahrenheit}°F`);
let fahrenheit2 = 86;
let celsius2 = (fahrenheit2 - 32) * 5/9;
console.log(`${fahrenheit2}°F is ${celsius2}°C`);
